<div class="users">
    <h1>Users</h1>
    {{#if hasUsers}}
        <ul class="user-list">
            {{#each users}}
                <li class="user-item">{{this.username}}</li>
            {{/each}}
        </ul>
    {{else}}
        <h1>No Users Found</h1>
    {{/if}}
</div>